import { createClientFromRequest } from 'npm:@base44/sdk@0.8.44';
export default async function() { return Response.json({error:'Usá Mercado Pago del conductor al finalizar el viaje.'},{status:409}); }
