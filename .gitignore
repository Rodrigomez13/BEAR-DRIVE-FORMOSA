# env files
.env
.env.*
!.env.example

# Logs
/logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Nitro build output, once this app has a server. Both are regenerated by
# every build, and the deploy runs one before reading them.
.output
.wrangler

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

.vite
base44/.app.jsonc

# Android/Capacitor is source code and must be versioned.
# Never commit release signing keys, Google Navigation secrets, or local machine config.
*.keystore
*.jks
android/local.properties
android/secrets.properties
android/.gradle/
android/**/build/
